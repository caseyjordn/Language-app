# Reference material (source docs, screenshots, the old app) — not needed
# to run the deployed app, since all that content is already transcribed
# into index.html's data arrays. Stays on your local disk, just not pushed.
files/

# Editor-specific, not part of the app
.vscode/

# Scratch files from development/build scripts, if ever left behind
_tmp_check.js
vocab-array-output.txt
vocab-body-only.txt
kanji-06-10-output.txt
kanji-verified-output.txt
sentences-output.txt
vocab-verified-output.txt
vocab-complete-output.txt
kanji-complete-output.txt
tourist-vocab-output.json

# One-time personal progress import, generated by migrate-duo-vocab.js —
# contains real account data, not something to publish
casey-progress-import.json
